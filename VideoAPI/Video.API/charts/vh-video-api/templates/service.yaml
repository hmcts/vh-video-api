apiVersion: v1
kind: Service
metadata:
  name: {{ template "videoapi.fullname" . }}
  labels:
    app: {{ template "videoapi.name" . }}
    chart: {{ template "videoapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "videoapi.name" . }}
    release: {{ .Release.Name }}
